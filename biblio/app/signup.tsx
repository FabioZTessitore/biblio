import { Text, StyleSheet } from "react-native";

const Signup = () => {
  return <Text style={styles.signup}>Signup Component</Text>;
};

const styles = StyleSheet.create({
  signup: {
    color: "red",
  },
});

export default Signup;
